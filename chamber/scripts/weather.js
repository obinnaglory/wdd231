document.addEventListener("DOMContentLoaded", () => {
  const weatherInfo = document.getElementById("weather-info");
  // Example static text (replace with API call later)
  weatherInfo.textContent = "Sunny, 28°C";
});
